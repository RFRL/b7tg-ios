name: Build B7 Telegram fork

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Write configuration with secrets
        run: |
          python3 - <<'PYEOF'
          import json
          with open("build-system/appstore-configuration.json") as f:
              cfg = json.load(f)
          cfg["bundle_id"] = "com.b7.telegramfork"
          cfg["api_id"] = "${{ secrets.TG_API_ID }}"
          cfg["api_hash"] = "${{ secrets.TG_API_HASH }}"
          cfg["team_id"] = "C67CF9S4VU"
          cfg["is_internal_build"] = "true"
          cfg["is_appstore_build"] = "false"
          cfg["appstore_id"] = "0"
          cfg["enable_siri"] = False
          cfg["enable_icloud"] = False
          cfg["premium_iap_product_id"] = ""
          with open("build-system/b7_build_configuration.json", "w") as f:
              json.dump(cfg, f, indent=2)
          PYEOF

      - name: Set active Xcode path
        run: |
          XCODE_VERSION=$(cat versions.json | python3 -c 'import json,sys;obj=json.load(sys.stdin);print(obj["xcode"]);')
          sudo xcode-select -s /Applications/Xcode_$XCODE_VERSION.app/Contents/Developer || sudo xcode-select -s /Applications/Xcode.app/Contents/Developer

      - name: Create canonical source directory
        run: |
          set -x
          sudo mkdir -p /Users/Shared
          cp -R $GITHUB_WORKSPACE /Users/Shared/
          mv /Users/Shared/$(basename $GITHUB_WORKSPACE) /Users/Shared/telegram-ios

      - name: Build the App
        run: |
          set -x

          SOURCE_DIR=/Users/Shared/telegram-ios
          BAZEL_USER_ROOT="/private/var/tmp/_bazel_containerhost"

          cd $SOURCE_DIR

          BUILD_NUMBER_OFFSET="$(cat build_number_offset)"
          export APP_VERSION=$(cat versions.json | python3 -c 'import json,sys;obj=json.load(sys.stdin);print(obj["app"]);')
          export COMMIT_COUNT=$(git rev-list --count HEAD)
          export COMMIT_COUNT="$(($COMMIT_COUNT+$BUILD_NUMBER_OFFSET))"
          export BUILD_NUMBER="$COMMIT_COUNT"

          python3 build-system/Make/ImportCertificates.py --path build-system/fake-codesigning/certs
          python3 build-system/Make/RewriteProfilesBundleId.py \
            --profilesPath build-system/fake-codesigning/profiles \
            --certsPath build-system/fake-codesigning/certs \
            --keychainName temp.keychain \
            --keychainPassword secret \
            --oldBundleId ph.telegra.Telegraph \
            --newBundleId com.b7.telegramfork
          python3 -u build-system/Make/Make.py \
            --bazelUserRoot="$BAZEL_USER_ROOT" \
            --overrideXcodeVersion \
            --overrideBazelVersion \
            build \
            --configurationPath="build-system/b7_build_configuration.json" \
            --codesigningInformationPath=build-system/fake-codesigning \
            --configuration=release_arm64 \
            --buildNumber="$BUILD_NUMBER"

          OUTPUT_PATH="$GITHUB_WORKSPACE/build/artifacts"
          rm -rf "$OUTPUT_PATH"
          mkdir -p "$OUTPUT_PATH"
          cp "$SOURCE_DIR/bazel-bin/Telegram/Telegram.ipa" "$OUTPUT_PATH/B7Telegram.ipa"

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: B7Telegram-ipa
          path: build/artifacts/B7Telegram.ipa
